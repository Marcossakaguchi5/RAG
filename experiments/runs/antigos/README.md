# Rodadas antigas

## SciQ

- `20260702-legacy/`: primeira saída plana, preservada apenas para auditoria.
- `20260707-183922/`: baseline preliminar canônica das tabelas e figuras atuais do
  artigo. Contém `retrieval/`, `results/`, `plots/`, `summary_test.json` e
  `manifest.json`.

Essas pastas são somente leitura do ponto de vista experimental: novas execuções
devem receber outro `run_id` em `../novas/`.
